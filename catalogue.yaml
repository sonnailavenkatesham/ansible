- name: install catalogue component
  hosts: catalogue
  become: yes
  tasks:
  -name: setup NPM sourece
    ansible.builtin.shell: "curl -sL https://rpm.nodesource.com/setup_lts.x | bash"
  
  - name: install nodejs
    ansible.builtin.yum:
      name: nodejs
      state: installed
